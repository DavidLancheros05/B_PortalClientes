REFACTORING DE MODALES NATIVOS (alert/confirm) A MODALES GENÉRICOS
====================================================================

ESTADO: COMPLETO (2026-09-18). Verificado con
  grep -rn "\balert(\|\bconfirm(" F_PortalClientes/src --include="*.tsx" --include="*.ts" | grep -v "// "
-> 0 resultados. Ningún alert()/confirm() nativo real queda en el código.

Ver GUIA_MODALES.md para el patrón a seguir. NOTA (2026-09-13): esa guía
está desactualizada en un punto — recomienda "error -> ConfirmModal con
isDangerous=true", pero el patrón vigente (ver
`rediseno-gestionar-comite-credito.md` y `rediseno-seguridad.md`) es usar
el componente `ErrorModal` real para errores, dejando ConfirmModal solo
para confirmaciones sí/no. Se siguió ErrorModal en toda esta última tanda.

Componentes fuente:
  F_PortalClientes/src/components/modals/ModalesGenericos.tsx
  (ConfirmModal, SuccessModal, LoadingModal, ErrorModal, WarningModal)

--------------------------------------------------------------------
ARCHIVOS COMPLETAMENTE REFACTORIZADOS
--------------------------------------------------------------------

- F_PortalClientes/src/app/parametrizacion/formularios/[formularioId]/versiones/page.tsx
  * alert() -> SuccessModal / ConfirmModal(isDangerous=true) via estado "notificacion"
  * confirm() -> ConfirmModal via estado "versionAConfirmar" (flujo "Activar versión")

- F_PortalClientes/src/app/parametrizacion/formularios/[formularioId]/nueva-version/page.tsx
  * alert() -> SuccessModal / ConfirmModal(isDangerous=true) via estado "notificacion"
    (SuccessModal navega a versiones/ en su onAction, igual que hacía el alert antes)

Todo el módulo seguridad/ (ver rediseno-seguridad.md para el detalle, 2026-09-13):
  - seguridad/modulos/page.tsx (12 sitios -> ConfirmModal/SuccessModal/ErrorModal)
  - seguridad/usuarios/page.tsx (5 sitios)
  - seguridad/usuarios/UsuarioCentrosModal.tsx (1 sitio)
  - seguridad/roles/page.tsx (el ConfirmModal-como-error ya existente pasó a ErrorModal real)
  - seguridad/usuario-roles/page.tsx (no tenía alert/confirm, pero se agregó
    ConfirmModal antes de quitar un rol asignado, que antes no confirmaba nada)
  - seguridad/consecutivos/consecutivos/page.tsx y
    seguridad/consecutivos/tipo-consecutivo/page.tsx (ya usaban ConfirmModal/
    SuccessModal; el ConfirmModal-como-error pasó a ErrorModal real)

Tanda final (2026-09-18), 26 archivos / 52 ocurrencias reales (conteo
recalculado a mano ese mismo día, corrigiendo un conteo de 53 con un error
en gestion-ejecutivo-negocios/[id]/registrar/page.tsx: eran 4, no 6):

  4  solicitudes/gestion-ejecutivo-negocios/[id]/registrar/page.tsx
     -> los 3 alert() de carga/guardado ya usaban ErrorModal; se migraron
        las 4 validaciones de formulario (usuario/consumo/toneladas/
        observaciones) a setErrorMessage.
  4  solicitudes/gestion-auxiliar-servicio-al-cliente/[id]/gestionar/page.tsx
     -> no tenía ErrorModal; se agregó import + estado errorMessage +
        <ErrorModal> para los 3 alert() (carga, usuario no autenticado,
        modo de solución, guardado).
  3  components/TablaPersonaConEvidencia.tsx
     -> confirm() de eliminar evidencia pasó a ConfirmModal con estado
        {filaIndex, sepId}; los 2 alert() (subir/eliminar) a ErrorModal.
  3  components/SoportesAnalisis.tsx
     -> mismo patrón: confirm() de eliminar -> ConfirmModal (ssaAEliminar);
        alert() de subir/eliminar -> ErrorModal.
  3  parametrizacion/formulario-editor/hooks/useSeccionEditor.ts
     -> hook sin JSX propio: se agregó errorMessageSeccion/
        setErrorMessageSeccion al hook y se propagó como prop hasta
        PanelSecciones.tsx (que ya renderiza Toast/ConfirmModal para
        successMessageSeccion — mismo módulo, mismo patrón, se sumó
        <ErrorModal> ahí en vez de un Toast porque error es bloqueante).
  3  solicitudes/[id]/detalle/page.tsx
     -> ya tenía un estado "error" pero es de carga de página completa
        (bloquea el render); se agregó "actionErrorMessage" aparte +
        ErrorModal para los 3 alert() de generación de PDF/carta.
  2  components/form/SolicitudForm.tsx
     -> componente HUÉRFANO: no lo importa ningún otro archivo del repo
        (confirmado con grep). Se migró igual por consistencia pero no se
        borró — decisión de borrarlo queda para cuando el usuario lo pida.
  2  parametrizacion/formularios/page.tsx
     -> confirm() de eliminar formulario -> ConfirmModal(formularioAEliminar);
        alert() de error -> ErrorModal.
  2  parametrizacion/formulario-preguntas/components/FormularioPreguntaTable.tsx
     -> confirm() de cambiar estado -> ConfirmModal; alert() -> ErrorModal.
        (se envolvió el return en <> porque ahora hay elementos hermanos
        de <table>).
  2  solicitudes/corregir-formulario-asc/page.tsx
  2  solicitudes/gestion-comite-credito-2/page.tsx
  2  solicitudes/gestion-comite-credito-1/page.tsx
  2  solicitudes/gestion-ejecutivo-negocios/page.tsx
  2  solicitudes/gestion-oficial-de-cumplimiento/page.tsx
  2  solicitudes/gestion-auxiliar-servicio-al-cliente/page.tsx
  2  solicitudes/rechazadas-ejecutivo/page.tsx (ya tenía LoadingModal/InfoModal)
     -> las 7 páginas de listado de arriba comparten un patrón idéntico:
        alert("No hay usuario autenticado") + alert("Error al
        cargar/buscar solicitudes") -> setErrorMessage + <ErrorModal>.
  2  solicitudes/gestion-comite-credito-1/[id]/gestionar/page.tsx
     -> ya tenía ConfirmModal/SuccessModal/ErrorModal/errorMessage
        armados; solo faltaba reemplazar los 2 alert() sueltos.
  1  solicitudes/gestion-comite-credito-2/[id]/gestionar/page.tsx
  1  solicitudes/gestion-oficial-de-cumplimiento/[id]/gestionar/page.tsx
     -> mismo caso: infraestructura ya lista, solo faltaba el alert() de
        "Error al cargar la solicitud".
  1  app/pqrs/bandeja/page.tsx
     -> ya tenía un estado "error" para carga de página; se agregó
        "actionErrorMessage" aparte + ErrorModal para el alert() de
        "tomar PQRS".
  1  solicitudes/nueva/components/ArchivoField.tsx
  1  solicitudes/nueva/components/DocumentoTablaField.tsx
  1  solicitudes/nueva/components/ImagenField.tsx
     -> los 3 comparten el mismo patrón "confirm antes de eliminar
        archivo/imagen"; se extrajo la lógica a una función
        handleEliminarArchivo/handleEliminarImagen separada del onClick y
        se agregó un ConfirmModal con estado booleano local.
  1  solicitudes/nueva/components/ArchivoMultipleField.tsx
     -> mismo patrón pero la función recibía un saId como parámetro
        (admite varios archivos por pregunta); el estado de confirm
        guarda ese saId (saIdAEliminar) en vez de un booleano.
  1  solicitudes/rechazadas-ejecutivo/[id]/page.tsx
     -> igual que detalle/page.tsx y pqrs/bandeja: ya tenía un "error" de
        carga de página: se agregó "actionErrorMessage" aparte para el
        alert() de "finalizar gestión".
  1  components/DocumentosCargadosSolicitud.tsx
     -> alert() de "actualizar fecha de emisión" -> setErrorMessage +
        ErrorModal (componente no tenía ningún modal antes).

Verificación aplicada a TODA la tanda: `npx tsc --noEmit` sin errores
después de cada grupo de archivos (no se corrió `npm run build`, ver
[[no-build-proactivo-solo-al-pushear]] — reservar el build para cuando se
vaya a pushear a git).

--------------------------------------------------------------------
NO son pendientes reales (falsos positivos del grep, ya migrados o texto
no ejecutable — no perder tiempo revisándolos si se vuelve a correr el grep):
  - seguridad/modulos/page.tsx y seguridad/usuarios/page.tsx: el match es
    un comentario ("reemplaza alert()/confirm() nativos"), no una llamada
    real — ya migrados, confirma [[migrar-alerts-nativos-a-modales]] /
    sistema-diseno-portal-clientes-parte-visual.
  - components/LoginForm.tsx: el match es una línea comentada (`//`).
  - REFACTOR_EXAMPLE.md: es este mismo documento de ejemplo, no código.

Para verificar que sigue en cero en el futuro:
  grep -rn "\balert(\|\bconfirm(" F_PortalClientes/src --include="*.tsx" --include="*.ts" | grep -v "// "
